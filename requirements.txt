fastapi
fastapi-offline
uvicorn
gunicorn
aiofiles
requests
httpx
python-multipart
ujson
msgpack
tqdm
icc-rt
numba
numpy<2
opencv-python
scikit-image
onnx==1.16.0
# 1.18.1 is the release paired with onnx 1.16; also the first pin whose wheels
# drop the executable-stack (GNU_STACK RWX) flag that newer kernels refuse,
# which crashed onnxruntime import in-cluster.
onnxruntime==1.18.1
PyTurboJPEG==1.7.5
exifread
tritonclient[http,grpc]
# transitive dep of tritonclient; >=1.21 has no py3.8 wheels (source build needs g++)
python-rapidjson<=1.20
imageio
# AVIF/HEIF decode fallback for decode_img_bytes (image proxies serving image/avif,
# iPhone HEIC uploads). register_heif_opener() covers both formats.
pillow-heif
aiohttp
tenacity
colorlog